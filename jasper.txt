PROMPT 2 — Delete all dead weight (lean + fast)
Run second You are making this codebase lean. Delete every dead file. No file listed here is imported by any active component in the live codebase. Verify with grep before deleting if uncertain. STEP 1 — Delete 4 dead section components (verify first, then delete):
# Verify each is truly unused:
grep -r "HeroSection" src/ --include="*.tsx" | grep -v "HeroSection.tsx" | grep -v "CinematicHeroSection"
grep -r "TreatmentsSection" src/ --include="*.tsx" | grep -v "TreatmentsSection.tsx" | grep -v "EditorialTreatmentsSection"
grep -r "TestimonialsSection" src/ --include="*.tsx" | grep -v "TestimonialsSection.tsx"
grep -r "CategoryGrid" src/ --include="*.tsx" | grep -v "CategoryGrid.tsx"

# If all return 0 results, delete:
rm src/components/sections/HeroSection.tsx
rm src/components/sections/TreatmentsSection.tsx
rm src/components/sections/TestimonialsSection.tsx
rm src/components/sections/CategoryGrid.tsx
STEP 2 — Remove 11 junk files from git tracking:
git rm --cached design.txt dev-log.txt dev-log2.txt jasper.txt lint-output.txt temp.log
git rm --cached tsconfig.tsbuildinfo package-lock.json
git rm --cached prisma/dev.db prisma/db/dev.db
git rm -r --cached download/
git commit -m "chore: untrack dev logs, db files, and build artifacts"
STEP 3 — Verify .gitignore covers all of these patterns:
*.log
temp.log
design.txt
dev-log*.txt
jasper.txt
lint-output.txt
tsconfig.tsbuildinfo
package-lock.json
prisma/*.db
prisma/db/
download/
Add any missing patterns to .gitignore. STEP 4 — Replace 2 SVG placeholders with real images:
# ConsultationCTA placeholder:
curl -L -o public/images/consultation.jpg \
  "https://images.unsplash.com/photo-1598300042247-d088f8ab3a91?w=1200&q=80"

# Testimonial portrait placeholder:
curl -L -o public/images/testimonial-portrait.jpg \
  "https://images.unsplash.com/photo-1508214751196-bcfd4ca60f91?w=600&q=80"
Then update references in: - src/components/sections/ConsultationCTA.tsx: change imageSrc="/images/consultation.svg" to "/images/consultation.jpg" - src/components/sections/PullQuoteTestimonial.tsx: change src="/images/testimonial-portrait.svg" to "/images/testimonial-portrait.jpg" STEP 5 — After all deletions, run build to confirm nothing broke:
bun run build
If build fails because something imported a deleted component, fix that import. Otherwise the deletions are clean.