PROMPT 5 — Elevate: consultation SVG → real image + SkinTrendsSection motion
A — ConsultationCTA uses consultation.svg (a 348-byte placeholder): The StickySplitSection receives imageSrc="/images/consultation.svg". An SVG placeholder in a sticky editorial layout looks wrong. Replace with a real image. In src/components/sections/ConsultationCTA.tsx, change:
imageSrc="/images/consultation.svg"
To:
imageSrc="https://images.unsplash.com/photo-1598300042247-d088f8ab3a91?w=1200&q=80"
(A woman receiving a facial treatment — clean, professional, appropriate.) B — PullQuoteTestimonial uses testimonial-portrait.svg (a 407-byte placeholder): In src/components/sections/PullQuoteTestimonial.tsx, change the Image src to:
src="https://images.unsplash.com/photo-1508214751196-bcfd4ca60f91?w=600&q=80"
(A tasteful portrait photo of a woman — appropriate for a beauty salon testimonial.) C — SkinTrendsSection: verify it uses the Link from @/i18n/routing (not next/link): Open src/components/sections/SkinTrendsSection.tsx. Check the Link import — it currently uses import Link from 'next/link' which won't add locale prefix. Change to:
import { Link } from '@/i18n/routing';
D — AboutSection: verify section-label is present: Open src/components/sections/AboutSection.tsx. Before the main <h2>, verify there is a div.section-label. If missing, add:
<div className="section-label mb-8">
  <span>{t('sectionLabel')}</span>
</div>
If about.sectionLabel doesn't exist in EN messages, add it with value "About Us" and translate into all 6 locale files. After all these changes: bun run typecheck && bun run lint && bun run validate:i18n