Analyze this Next.js App Router repo and remove dead code safely.

Goals:
- Make the codebase leaner without changing visible behavior.
- Prefer deletion over refactor where code is clearly unused.
- Keep TypeScript strict and keep App Router conventions.
- Run lint and typecheck after changes.

Tasks:
1. Confirm and delete orphaned/generated files that are not used by runtime:
   - middleware.zip
   - repomix-output.xml
   - src/app/[locale]/contact-us/page-server.tsx
2. Audit src/components/sections and delete old homepage/editorial section files that are no longer imported by any live route.
3. If those old sections are removed, also delete now-unused files in src/components/effects.
4. Delete obviously unused utility/provider files if they have no runtime imports:
   - src/components/providers/performance-provider.tsx
   - src/lib/image-optimizer.ts
   - src/lib/gdpr.ts
5. Remove any barrel exports that become stale after deletion.
6. Do not change product behavior unless required by the deletion.
7. At the end, report:
   - deleted files
   - imports cleaned up
   - any files that looked unused but were kept due to uncertainty

Validation:
- npm run lint
- npm run typecheck
