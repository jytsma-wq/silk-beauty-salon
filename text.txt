Prompt 7 — Elevate: add ScrollReveal to all homepage sections
Import ScrollReveal from @/components/ScrollReveal in src/app/[locale]/page.tsx. Wrap each section with a staggered ScrollReveal: ContentSection at delay 0, CategoryGrid at delay 0.1, StatsSection at delay 0, CTABanner at delay 0, BeforeAfter at delay 0.1. Review each section component and ensure they don't have conflicting opacity or transform styles that would fight the animation. Also add ScrollReveal to CTABanner.tsx and CategoryGrid.tsx internally for child card elements to stagger in. Verify framer-motion is listed in dependencies — it is ("framer-motion": "^12.38.0").